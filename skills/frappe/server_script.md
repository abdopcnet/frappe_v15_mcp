Server Script: run Python on doc event or via button; no app deploy needed.

```python
# In Server Script doc: set doctype, script type (Doc Event / API), and Python code.
# doc = frappe.get_doc("Task", doc.name)
```

CRUD via GET/POST /api/resource/{DocType}. Use whitelisted methods for custom logic.

```python
# Client: GET /api/resource/Task?filters=[["status","=","Open"]]
# Or call whitelisted method for complex logic.
```

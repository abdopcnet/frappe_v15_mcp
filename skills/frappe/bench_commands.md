Common bench commands: new-site, use, migrate, backup, restore, run-tests.

```bash
bench new-site site.local
bench use site.local
bench migrate
bench backup
bench restore backup_file.sql.gz
bench run-tests --app my_app
```

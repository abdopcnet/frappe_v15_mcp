Client Script: set DocType and script; runs in browser for that form/list.

```javascript
// In Client Script doc: doctype = "Task", script = "frappe.ui.form.on('Task', { refresh: (frm) => {} });"
```

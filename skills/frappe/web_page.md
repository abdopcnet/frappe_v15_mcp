Public web page: create Web Page doc or route in hooks.

```python
# hooks.py
website_route_rules = [{"from_route": "/custom-page", "to_route": "custom-page"}]
# Or use Web Page DocType with HTML/Markdown.
```

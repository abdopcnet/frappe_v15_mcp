# refresh_field

Refresh form field(s) after value or table changes.

```javascript
frm.refresh_field("field_name");
frm.refresh_fields(["field1", "field2"]);
```

Always refresh child table fields after `add_child` or `clear_table`.

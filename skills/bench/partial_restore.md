# partial-restore

Restore selected data from a backup SQL file.

```bash
bench --site <site> partial-restore <backup.sql>
```

## Notes

- Use when full restore is not required
- Validate data consistency after restore

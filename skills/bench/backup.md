# Backup

Use this when you need a site backup before risky operations.

## Example

```bash
bench --site site.local backup
```

## Common variant

```bash
bench --site site.local backup --with-files
```

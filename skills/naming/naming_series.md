# naming_series

Auto-generate names from a series.

DocType: `naming_rule`: "By Naming Series", `autoname`: "naming_series:". Field options: `SINV-.YYYY.-`, `SINV-.YY.-`, `CUST-`. In code: `doc.naming_series = "SAL-.YYYY.-"` then `doc.set_new_name()`.

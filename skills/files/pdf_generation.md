# PDF Generation

Use one PDF utility call to generate a PDF from HTML content.

```python
from frappe.utils.pdf import get_pdf
pdf_bytes = get_pdf("<h1>Invoice</h1>")
```

- Keep HTML minimal and valid
- Use print formats for complex layouts
- Attach generated bytes as a file

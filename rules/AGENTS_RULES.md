# Follow AGENTS.md: investigate before coding; never modify frappe/erpnext/hrms.

## Rules (v15)

- **Investigate before coding** — understand and analyse first. When following: end reply with ✅.
- **Frappe/ERPNext** — follow their standards; English-only short comments above code.
- **Never modify** — `frappe`, `erpnext`, `hrms`.

## Naming

- Python: `snake_case` (functions, variables).
- JavaScript: `camelCase` (variables), `PascalCase` (classes).
- DocType: Title Case with spaces.
- Files: `snake_case.py`, `snake_case.js`.

## Logging

- Frontend: `skills/logging/frontend_logging.md`.
- Backend: `skills/logging/log_error.md`.

## Index

- Full list: repo root `llms.txt` and `AGENTS.md` (apps folder).
